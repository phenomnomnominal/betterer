/**
 * {@link https://knip.dev/ | Knip} test for {@link https://github.com/phenomnomnominal/betterer | **Betterer**}.
 *
 * This test is currently in *beta* since the underlying {@link https://knip.dev/ | Knip}
 * implementation is based on private APIs. Will stabilise when it can be migrated to
 * use public APIs.
 *
 * @packageDocumentation
 */

export { knip } from './knip.js';
export { KnipConfig, KnipCLIOptions } from './types.js';
